#!/usr/bin/env python3
"""
Drug Discovery AI Platform - Main Pipeline
"""

import os
import json
import sys

print("="*60)
print("Drug Discovery AI Platform")
print("="*60)

# Check components
components = {
    'Visualizations': '01_Visualizations',
    'Neural Networks': '02_Neural_Networks',
    'AGI Components': '03_AGI_Components'
}

for name, path in components.items():
    if os.path.exists(path):
        files = len([f for f in os.listdir(path) if os.path.isfile(os.path.join(path, f))])
        print(f"{name}: {files} files")
    else:
        print(f"{name}: Not found")

# Load AGI configuration
if os.path.exists('03_AGI_Components/neural_agi_system_config.json'):
    with open('03_AGI_Components/neural_agi_system_config.json', 'r') as f:
        config = json.load(f)
    print(f"\nAGI System: Active")

print("\nPipeline ready!")